defmodule Github do
  def org_url(_org_name), do: "TODO: org_url"
  def repo_url(_owner_name, _repo_name), do: "TODO: repo_url"
  def org(org_name), do: "TODO: org"
  def repo(owner_name, repo_name), do: "TODO: repo"
end